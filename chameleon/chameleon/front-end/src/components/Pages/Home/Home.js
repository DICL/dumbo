import React from 'react';
import ComputeNodes from '../../ComputeNodes/ComputeNodes'
import Modals from '../../Modal';

const Home = () => {
    return (
      <div>
        <ComputeNodes/>
        <Modals/>
      </div>
    );
};

export default Home;
