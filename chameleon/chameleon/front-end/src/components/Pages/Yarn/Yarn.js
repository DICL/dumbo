import React from 'react';
import AppMonitor from '../../AppMonitor/AppMonitor'
import Modals from '../../Modal';

const Yarn = () => {
    return (
      <div>
        <AppMonitor/>
        <Modals/>
      </div>
    );
};

export default Yarn;
