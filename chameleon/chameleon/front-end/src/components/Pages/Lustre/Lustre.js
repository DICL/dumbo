import React from 'react';
import './Lustre.css';
import LustreFS from '../../LustreFS';

const Lustre = () => {
    return (
        <div>
            <LustreFS/>
        </div>
    );
};

export default Lustre;
